@model IEnumerable<RecipeForSuccess.ViewModels.IngredientVM>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table" id="#ingredientsTable">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MeasureValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IngredientName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MeasureValue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IngredientName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }

</table>
